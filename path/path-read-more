## 2. Node JS architecture

    1. Explain briefly about LIBUV - https://codedamn.com/news/nodejs/libuv-architecture

## Path module

    1. codevolution - youtube channel
    2. https://www.scaler.com/topics/nodejs/path-module-in-node-js/
    3. https://www.geekster.in/articles/path-module-in-node-js/
    4. https://nodejs.org/en/learn/manipulating-files/nodejs-file-paths
    5. https://nodejs.org/api/path.html

## Absolute vs Relative path

    In Node.js, path.isAbsolute(path) is a method from the path module that checks whether a given path is an absolute path. An absolute path is a complete path that specifies the location of a file or directory from the root of the filesystem, whereas a relative path specifies the location in relation to the current working directory.

## Rules to be an absolute path

    1.  An absolute path begins with a forward slash (/) in UNIX operating systems like Linux, MacOS etc.
        Eg: /home/user/documents/file.txt

    2.  It can start with a drive letter (like C, D, etc.) followed by a colon (:) and a backslash (\).
        Eg: C:\Users\username\documents\file.txt

    3.  A UNC (Universal Naming Convention) path can also be considered absolute. These paths are used to access network    resources. Eg: \\Server\Share\Documents\file.txt

    4.  An absolute path should not include components like . (current directory) or .. (parent directory) that denote relative positioning.